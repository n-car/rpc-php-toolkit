{
    "name": "rpc-php-toolkit/rpc-php-toolkit",
    "description": "PHP JSON-RPC 2.0 client/server toolkit with middleware, schema validation, batch processing, introspection, and optional Safe Mode",
    "type": "library",
    "keywords": [
        "json-rpc",
        "rpc",
        "server",
        "client",
        "middleware",
        "validation",
        "bigint",
        "safe-mode",
        "introspection",
        "json-rpc-2.0",
        "php",
        "toolkit"
    ],
    "license": "MIT",
    "authors": [
        {
            "name": "Nicola Carpanese",
            "email": "nicola.carpanese@gmail.com"
        }
    ],
    "require": {
        "php": ">=8.0",
        "ext-json": "*",
        "ext-mbstring": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.0",
        "squizlabs/php_codesniffer": "^3.6",
        "phpstan/phpstan": "^1.0"
    },
    "autoload": {
        "psr-4": {
            "RpcPhpToolkit\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "RpcPhpToolkit\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "test-coverage": "phpunit --coverage-clover coverage.xml --coverage-html coverage",
        "phpcs": "phpcs src/ --standard=PSR12 --ignore=src/clients/*.js",
        "phpstan": "phpstan analyse src/ --level=8"
    },
    "config": {
        "sort-packages": true
    }
}
